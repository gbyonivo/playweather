import * as Functions from '../../src/functions';
import { forecast } from '../__testData__';

describe('', () => {
  it('', () => {
    const actual = Functions.forecastGraphData(forecast);
    const expected = [];
    expect(actual).toMatchObject(expected);
  });
});